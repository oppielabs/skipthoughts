from setuptools import setup

setup(
   name='skipthoughts',
   version='0.0.1',
   description='Sent2Vec encoder and training code from the paper Skip-Thought Vectors.',
   packages=['skipthoughts']
)